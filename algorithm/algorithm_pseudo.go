// Algoritmen bør mates med en oversikt over 
// Heisnr:, state:, last:, 

// Når algoritmen får en ekstern desinasjonsordre
Sjekk hvilken state heisen er i 

Gå gjennom oversikten å sjekk heis state
if standby
	// Det er logisk å benytte seg av en standby heis for å holde alle heiser aktive
else
	// Dersom alle heisene er aktive må man finne hvilken som går mot destinasjon
	// Sjekk last opp mot ordre
	if noen av heisene er på vei mot ordre
		// Prioriter disse
	else 
		